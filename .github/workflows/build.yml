on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: cachix/install-nix-action@v17
      - run: nix-build -A gtk3 https://github.com/bobby285271/nixpkgs/archive/4e323c2ae12b3801b502d86b2af7b72365c9792e.tar.gz
